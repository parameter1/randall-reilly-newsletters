import defaultValue from "@base-cms/marko-core/utils/default-value";

$ const style = {
  "font-size": "1px",
  "line-height": "1px",
  ...input.style,
};

$ const attributes = {
  align: "center",
  valign: "top",
  height: defaultValue(input.height, "12"),
  ...input.attributes,
  style,
};

$ if (input.log) console.log(attributes);

<tr>
  <td ...attributes>&nbsp;</td>
</tr>
